<Window x:Class="KickBlastStableLight.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="KickBlast Login" Height="430" Width="440"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="{StaticResource BrushAppBackground}">
    <Grid>
        <Border Style="{StaticResource CardBorderStyle}" Width="320" HorizontalAlignment="Center" VerticalAlignment="Center">
            <StackPanel>
                <TextBlock Text="KickBlast Stable Light" FontSize="22" FontWeight="Bold"/>
                <TextBlock Text="Login to continue" Foreground="{StaticResource BrushMuted}" Margin="0,4,0,20"/>

                <TextBlock Text="Username"/>
                <TextBox x:Name="TxtUsername" Style="{StaticResource RoundedTextBox}" Margin="0,6,0,8" Text="dinidu"/>
                <TextBlock Text="Password"/>
                <PasswordBox x:Name="TxtPassword" Margin="0,6,0,6" Padding="10,6"/>
                <TextBlock x:Name="TxtError" Foreground="{StaticResource BrushDanger}" Margin="0,4,0,10"/>

                <Button x:Name="BtnLogin" Content="Login" Style="{StaticResource PrimaryButtonStyle}" Click="BtnLogin_Click"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
